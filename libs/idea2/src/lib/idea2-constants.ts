/**
 * Used to signal that a resource property should use the CDK generated value.
 * For example: when deploying an Cloudfront deployment without a domain name.
 */
export const GENERATED = Symbol('GENERATED');
